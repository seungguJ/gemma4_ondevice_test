# project.md

## 목적

이 문서는 Codex와 Claude가 함께 사용하는 작업 현황판이다.

- 현재 진행 작업
- 담당자
- 상태
- 최근 결정 사항
- 다음 액션

## 시작 전 참고 순서

작업 시작 전 기본 확인 순서는 아래와 같다.

1. `project.md`
2. `docs/collab_protocol.md`
3. 본인 역할 문서 (`AGENTS.md` 또는 `CLAUDE.md`)
4. `business_context.md`
5. `design.md`
6. `README.md`
7. 관련 `docs/modules/*.md`

## 상태 규칙

- `todo`: 시작 전
- `in_progress`: 진행 중
- `review`: 구현 완료, 검토 대기
- `blocked`: 외부 확인 필요
- `done`: 검증과 문서 반영까지 완료

## 현재 작업 현황

| ID | 작업명 | 담당 | 상태 | 산출물 | 비고 |
|---|---|---|---|---|---|
| P-001 | 협업 문서 체계 수립 | Codex | done | `AGENTS.md`, `CLAUDE.md`, `project.md`, `business_context.md`, `design.md`, `docs/collab_protocol.md`, `docs/template.md` | 초기 운영 문서 |
| P-002 | 앱 대표 이미지 개선 반영 | Claude | done | 앱 아이콘 및 UI 이미지 교체 | 빌드 검증은 Java 환경 필요 |
| P-003 | Codex-Claude 자동 인계 실행층 추가 | Codex | done | `docs/agent_runtime.md`, `scripts/claude_handoff.sh`, 협업 문서 갱신 | 문서 규약을 실제 CLI 호출 흐름으로 연결 |
| P-004 | Git 업로드 제외 규칙 및 개인정보 제거 | Codex | done | `.gitignore`, `CalendarReader.kt`, 관련 문서 갱신 | 로컬 산출물과 하드코딩 이메일 제거 |
| P-005 | 일정 기준시각 및 Wallet 월 누적 보정 | Codex | blocked | 일정 필터링, Wallet 월별 집계 단순화, 관련 문서 갱신 | 구현 반영 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-006 | 정기결제 후보 알림 POC | Codex | blocked | 금융 알림 필터 확장, 충전 완료 시 AI 배치 분석, 정기결제 scene | 구현 반영 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-007 | 금융 인사이트 1차: 후보 통합, AI 카테고리 분석, 시각화 | Codex | blocked | 카드 인사이트 후보 집합, CardExpenseInsightWorker, WalletInsightTab 시각화 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-008 | 금융 인사이트 2차: 채팅 내부 데이터 질의, 일정-금융 연결 | Codex | blocked | 채팅 내부 데이터 주입, ScheduleFinanceContextBar | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-009 | 알림 원본 저장소 통합 및 기능별 파생 상태 정리 | Codex | blocked | 공통 notification inbox, 기존 store adapter 정리, 관련 문서/상태판 갱신 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-010 | 카드 인사이트 월간 보관 정책 및 전월 비교 정리 | Codex | blocked | 홈 인사이트 제거, 카드 인사이트 월간 summary history, TC/단위 테스트, 문서 갱신 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드/테스트 실행 대기 |
| P-011 | 기기 전체 앱 사용 로그 DB 및 뷰어 추가 | Codex | blocked | Usage Access 기반 세션 수집, SQLite 저장소, 앱 내부 로그 뷰어, 개인화 계획 문서 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-012 | 카드 인사이트 수동 강제분석 및 분석 내역 표시 | Codex | blocked | 수동 분석 게이트 조정, 거래 내역 일회성 포함, 인사이트 내 분석 상태 목록, 관련 문서 갱신 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-013 | 알림 수신 시 AI 자동 실행 게이트 보정 | Codex | blocked | 정기결제/카드 인사이트 자동 분석 24시간 지연 예약, 모델 경합 방지, 관련 문서 갱신 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-014 | 모듈별 파일 구조와 AI 런타임 아키텍처 기준 수립 | Codex | done | `ARCHITECTURE.md` 모듈 구조, 의존 방향, AI 모델 불변 조건, 성능/변경 체크리스트 | 문서 기준 수립 완료 |
| P-015 | 모델 런타임 게이트 1차 적용 | Codex | blocked | `ModelRuntimeGate`, 자동 로드 제거, Worker 모델 실행 중앙화 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-016 | 카드 내역-인사이트 금액 표시 및 리포트 진입 통합 | Codex | blocked | 카드 사용내역 누적 금액을 인사이트 분석 기준 금액으로 표시, 별도 인사이트 탭 제거, 내역 내 분석 리포트 토글 | 구현 및 정적 리뷰 완료, `JAVA_HOME` 부재로 빌드 검증 대기 |
| P-017 | DB/스토리지 표와 module view 최신화 | Codex | done | `README.md`, `ARCHITECTURE.md`, `docs/modules/*`, `docs/diagrams/module-view.*` | 구현 기준 저장소/테이블/화면 흐름 문서 갱신 완료, `JAVA_HOME` 부재로 Gradle 테스트는 실행 불가 |

## 최근 결정

- Codex는 관리자 역할, Claude는 실무자 역할로 분리한다.
- 역할 문서는 `AGENTS.md`, `CLAUDE.md` 두 파일만 유지한다.
- 진행 상태 공유의 단일 기준 문서는 `project.md`다.
- 협업 프로토콜은 `docs/collab_protocol.md`에서 관리한다.
- 자동 위임은 문서만으로 활성화하지 않고 `docs/agent_runtime.md`와 `scripts/claude_handoff.sh`를 함께 유지한다.
- 로컬 설정, 에이전트 로그, 대형 모델 파일, 개인 계정 정보는 Git 추적 대상에서 제외한다.
- 일정 조회와 Wallet 월 집계의 기준 시간대는 `Asia/Seoul`로 통일한다.
- Samsung Wallet 알림은 카드명 없이도 승인/취소 금액만으로 당월 누적을 계속한다.
- 정기결제 후보 알림 POC는 지원 패키지 화이트리스트, `광고` 제외, `결제` 포함 규칙을 우선하고 AI 분석은 `충전 중 + 배터리 100%`일 때만 수행한다.
- 정기결제 AI 분석이 한 번 완료되면 다음 분석은 성공 시각 기준 24시간 이후에만 허용한다.
- 카드 인사이트 AI 분석도 정기결제와 동일하게 `충전 중 + 배터리 100%`일 때만 수행하고, 한 번 완료되면 다음 분석은 성공 시각 기준 24시간 이후에만 허용한다.
- 카드 사용내역의 분석 입력은 Samsung Wallet 거래 전체가 아니라 정기결제 후보와 동일한 규칙 기반 후보 집합을 우선 사용한다.
- 카드 사용내역 화면은 단순 월 합계 외에 AI 기반 카테고리 요약과 시각화 데이터를 함께 제공하는 방향으로 확장한다.
- 채팅, 홈, 일정은 분리된 화면으로 유지하되 금융 인사이트/내부 데이터 조회 결과를 서로 참조할 수 있게 연결한다.
- 알림 원본 데이터는 기능별 저장소마다 중복 보관하지 않고, 공통 inbox 저장소를 기준으로 Wallet/정기결제/카드 인사이트 상태를 파생 관리하는 방향으로 정리한다.
- 홈 화면의 별도 `이번 달 인사이트` 블록은 제거하고, 홈은 핵심 진입 카드 중심으로 유지한다.
- 카드 인사이트 raw 후보는 공통 notification inbox에 당월만 유지하고, 월이 바뀌면 전월 raw는 삭제하되 월간 합계/카테고리 합계/요약만 별도 history로 최대 12개월 보관한다.
- 카드 인사이트 테스트 케이스는 실제 런타임 DB 파일이 저장소에 없으므로 `NotificationInboxEntry` 스키마와 `WalletParserTest` 표본 알림을 기준으로 관리한다.
- 기기 전체 앱 사용 개인화는 우선 모델 없이 `Usage Access -> 세션 DB 적재 -> 뷰어` 단계까지 구현하고, 추천 모델은 후속 작업으로 분리한다.
- 기기 전체 앱 사용 로그는 현재 `사용자 지정 packageName 우선 allowlist + 앱 라벨 fallback` 기준으로 저장한다.
- 기기 전체 앱 사용 raw 세션은 최대 7일만 보관하고, 추천 정책은 최근 7일 데이터 기반 `next app` 추천을 1차 목표로 한다.
- 앱 사용 로그 뷰어는 현재 보관 중인 7일 세션을 `next app` 학습 예제 CSV로 내보내 후속 모델 학습 테스트에 사용할 수 있게 한다.
- 카드 인사이트 화면의 `지금 분석`은 수동 강제분석으로 간주해 배터리 게이트와 24시간 쿨다운을 건너뛴다.
- 카드 인사이트 수동 강제분석은 이번 보정 용도로 저장된 카드 거래 내역 중 미분석 거래도 일회성 입력에 포함하되, 이후 자동 분석은 공통 inbox 카드 인사이트 후보 기준으로 독립 진행한다.
- 카드 인사이트 화면에는 분석 내역 목록을 두어 알림 후보와 거래 내역별 분석 완료/대기 상태를 확인할 수 있게 한다.
- 카드 인사이트 카테고리는 소비지출 대분류를 앱 용도에 맞게 축약한 고정 목록을 사용하고, AI가 목록 밖 카테고리를 반환하면 `기타`로 정규화한다.
- 카드 인사이트 AI 응답이 비어 있으면 compact prompt로 1회 재시도하고, 그래도 실패하거나 JSON 해석에 실패하면 Android Logcat에 응답 일부를 남기고 화면에는 실패 사유를 표시한다.
- 정기결제/카드 인사이트 자동 분석은 알림 수신 직후 모델을 실행하지 않고 후보만 저장한 뒤 24시간 지연 작업으로 예약한다. 예약된 작업도 `충전 중 + 배터리 100%`와 모델 미사용 상태일 때만 모델을 로드한다.
- 프로젝트가 커질수록 새 파일은 UI, coordinator, rules, store, worker, model runtime 책임을 섞지 않고 모듈별 경계에 맞춰 배치한다.
- `LlmEngine` 직접 호출은 점진적으로 `ModelRuntimeGate` 같은 단일 게이트 계층으로 모아 모델 로드/해제/경합 정책을 중앙화한다.
- 앱 시작, 앱 복귀, 모델 import/download 완료 직후 자동 모델 로드는 금지한다. 사용자가 직접 로드하거나 허용 조건을 통과한 Worker만 모델을 로드한다.
- 카드 사용내역 화면의 `이번 달 누적` 금액은 인사이트 분석 결과 금액이 있으면 해당 금액을 우선 표시하고, 아직 분석 결과가 없으면 인사이트 후보 수집 금액을 사용해 내역과 인사이트의 금액 기준을 맞춘다.
- 카드 인사이트는 별도 탭으로 노출하지 않고, 카드 사용내역 `이번 달 누적` 금액 옆 `분석 리포트 보기` 버튼으로 기존 인사이트 리포트/분석 내역을 펼쳐 확인한다.
- 현재 실제 SQLite DB는 앱 사용 로그의 `app_usage_logs.db`뿐이며, Wallet raw 알림/거래/인사이트/정기결제 분석 상태는 SharedPreferences JSON 저장소로 유지한다.
- module view는 금융 인사이트, 정기결제 분석, 앱 사용 로그, Wallet SharedPreferences, App Usage SQLite를 별도 모듈/저장소로 표현한다.

## 꼭 체크해야 할 목록

- 온디바이스 AI 모델은 어떤 기능에서도 평소 상주 상태로 유지하면 안 된다. 상시 로드, 선로드 유지, 백그라운드 상주 모델은 금지한다.
- 알림 수신, 화면 진입, 데이터 저장만으로 실시간 AI 분석을 실행하면 안 된다. 알림 수신 직후에는 후보 저장과 예약까지만 허용한다.
- 정기결제/카드 인사이트 AI 분석은 `24시간 대기 + 충전 중 + 배터리 100% + 다른 화면/Worker가 모델 미사용` 조건을 모두 만족할 때만 자동 실행한다.
- 사용자가 명시적으로 누른 `지금 분석`, `재분석`, `전체 재분석` 같은 강제분석 액션만 배터리 게이트와 24시간 대기를 우회할 수 있다.
- AI 분석, 일정 요약, 채팅 응답처럼 실제 추론이 필요한 시점에만 모델을 로드한다.
- 추론이 끝나면 세션 정리와 함께 `LlmEngine.free()` 경로가 보장되어야 한다.
- 백그라운드 Worker는 기존 화면이 모델을 사용 중일 때 경쟁 로드를 시도하지 않아야 한다.
- 새 기능 추가 시 배터리, 메모리, 화면 생명주기에서 모델 해제 경로를 먼저 확인한다.

## 다음 작업 작성 규칙

새 작업을 추가할 때 아래 항목을 같이 기록한다.

- 작업 목적
- 시작 조건
- 완료 조건
- 관련 문서
- 관련 코드 파일

## 완료 정의

- 요구사항 반영 완료
- 기본 검증 완료
- 리스크 기록 완료
- 필요한 문서 갱신 완료
